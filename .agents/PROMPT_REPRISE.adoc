= PROMPT_REPRISE — container-provision-gradle
:session: 007
:date: 2026-07-14
:etat: Rotation LLM api-key-pool intégrée dans bakery-plugin. 13/13 fichiers traduits. Rendu AsciiDoc cassé.
:epic_actif: CP-1d — Corriger parser AsciiDoc + lancer traduction complète 635 fichiers

== Contexte de fin (S006)

Abandon Colab/Playwright. Rotation LLM locale via api-key-pool (borough Liberty).

bakery-plugin (commit `ac92cf9` sur cccp-education/bakery-gradle) :
- BOM 0.0.8 + api-key-pool:0.0.1 + i18n-contracts:0.0.2
- `IaConfig.portRange: IntRange?` — configurer la plage de ports
- `PooledOllamaLlmService` — rotation round-robin via `ApiKeyPool.getNextKey()`
  + fallback (essaie tous les ports si un échoue)
- `ContentTaskRegistrar` — utilise le pool si `portRange` set, sinon single endpoint

container-provision-gradle (commit `75906f1`) :
- Notebook Colab : magic commands indentées corrigées (subprocess.run)

Validation : 13/13 fichiers traduits, 0 erreurs, gemma4:31b-cloud, 5m45s.

== Reprendre à — Bug parser AsciiDoc + traduction complète

. Corriger le bug `PivotArticle` parser/renderer :
   - Titre devient `title=` vide dans le rendu traduit
   - Contenu aplati (frontmatter + body sur une ligne)
   - Voir `bakery.pivot.AsciiDocParser` + `bakery.pivot.AsciiDocRenderer`
. Lancer traduction complète 635 fichiers :
   - `gradle migrateContentI18n -PsiteDir=/tmp/cheroliv-site -PtargetLangs=en`
   - Temps estimé ~5h (635 fichiers × plusieurs blocs × LLM)
. Copier `content-i18n/en/` vers `office/sites/cheroliv.com/content-i18n/en/`
. Commit + push sur `cheroliv/office.git`

== Fichiers touchés (S006)

- `colab/start-ollama-tunnel.ipynb` — magic commands → subprocess.run
- `bakery-plugin/build.gradle.kts` — BOM 0.0.8 + api-key-pool
- `bakery-plugin/src/main/kotlin/bakery/llm/IaConfig.kt` — portRange
- `bakery-plugin/src/main/kotlin/bakery/llm/PooledOllamaLlmService.kt` — nouveau
- `bakery-plugin/src/main/kotlin/bakery/ContentTaskRegistrar.kt` — pool wiring

== Commits

- `75906f1` fix(colab): replace indented magic commands with subprocess.run
- `ac92cf9` feat(i18n): integrate api-key-pool for round-robin LLM rotation