== Mission Session 004 — Relancer notebook Colab (BOM 0.0.6 + bakery 0.0.3)

=== Contexte
Session 013 (workspace-bom) : BOM 0.0.6 publié sur Central. SEC-1 audit
terminé (0 coupable). Notebook Colab mis à jour : BOM 0.0.6 + port 11440.

=== Étapes
1. Ouvrir `colab/start-ollama-tunnel.ipynb` sur Google Colab (runtime GPU)
2. Exécuter toutes les cellules :
   - Installation Ollama + pull modèle `translategemma:4b`
   - Détection GPU (`nvidia-smi`)
   - Clone `cheroliv/office.git` via secret `REPO_OFFICE`
   - Gradle `migrateContentI18n` avec bakery-plugin 0.0.3 (résolu via BOM 0.0.6)
   - Push `content-i18n/` sur `cheroliv/office.git` (branche main)
3. Vérifier que `content-i18n/` est créé avec traductions fr→en
4. Si OK, ajouter langues cibles (es, ar, zh, etc.)

=== Contexte technique
- BOM 0.0.6 sur Central (bundle 3b886b28)
- bakery-plugin 0.0.3 sur Central (publié session 012)
- Notebook : buildscript + platform("workspace-bom:0.0.6") + apply(bakery)
- Port Ollama : 11440 (était 11434 INTERDIT, corrigé session 013)
- Site : `cheroliv/office.git` → `sites/cheroliv.com/jbake/content/`
- Source : fr, target : en
- Modèle : `translategemma:4b`
- Secrets Colab : `REPO_OFFICE`, `site_name`, `github-api-key-cheroliv`

=== Note
Le notebook ne peut pas être exécuté depuis CLI — il faut Colab runtime GPU.