= COMPLETED_TASKS_ARCHIVE — Juin 2026 — container-provision-gradle

== Sessions archivées

=== S000 — Bootstrap gouvernance + code initial
- Création structure projet (settings.gradle.kts, build.gradle.kts)
- ContainerProvisionPlugin.kt (3 tâches : installPlaywright, npmInstallPlaywright, provisionColabTranslation)
- ContainerProvisionExtension.kt (DSL : colabNotebookUrl, targetLangs, siteRepoUrl)
- playwright/colab-worker.mjs (Playwright Chromium → Colab)
- colab/start-ollama-tunnel.ipynb (notebook auto-content)
- .agents/ (AGENT, INDEX, BACKLOG, PROMPT_REPRISE, SESSIONS_HISTORY)
- Enregistrement dans AGENTS.adoc racine
- Git init + push sur cccp-education/container-provision-gradle

== Points ouverts à reporter

- CP-1 Playwright Colab auth Google
- CP-2 Tests
- CP-6 Publication Maven Central

=== S001 — Colab translation worker (2026-06-29)
- Notebook `start-ollama-tunnel.ipynb` réécrit (14 commits)
- Docker abandonné (nested overlayfs + iptables bloqués dans Colab)
- Ollama installé directement (`curl | sh` + `zstd`)
- Cache Google Drive (modèle, Gradle 9.6.1, .m2, GRADLE_USER_HOME)
- Secrets Colab : REPO_OFFICE, site_name, github-api-key-cheroliv
- JDK 25 Temurin (Eclipse Adoptium)
- Repo correct : `cheroliv/office.git` → `sites/cheroliv.com/jbake/content/`
- *BLOCAGE* : `org.jetbrains:annotations` 26.0.2-1 vs 17.0.0 sur classpath plugin
  → `configurations.all { force }` ne couvre pas le classpath plugin
  → `buildscript {} + apply()` casse l'extension type-safe `bakery {}`
  → Piste : init.gradle.kts ou exclusion duct-tape ou bakery 0.0.3

=== S002 — BOM 0.0.5 : résolution conflit annotations (2026-06-29)
- workspace-bom 0.0.4 → 0.0.5 publié sur Maven Central (bundle 77e1f71b)
- 11 boroughs plugins + jetbrains-annotations:26.0.2-1 dans le BOM (toml)
- Notebook Colab : pattern buildscript + platform() + apply() + extensions.configure
- Corrections : gradlePluginPortal() dans buildscript, String pas File pour sourceDir/outputDir
- container-provision-plugin : platform() BOM + Playwright géré par BOM
- Test local validé : migrateContentI18n BUILD SUCCESSFUL, zéro conflit annotations
- *RESTE* : bakery-plugin 0.0.2 sur Central n'a pas le code i18n récent → publier 0.0.3

=== S004 — Playwright + Colab i18n cheroliv.com (2026-06-30)
- Notebook Colab : 3 bugs corrigés
  ** `dryRun = false` dans DSL bakery (sinon copie sans traduire)
  ** Port `11434` natif (Colab isolé, pas de contrainte de plage)
  ** Gradle copié vers `/content/gradle-local` (Drive monté noexec → PermissionError)
- ColabTranslationTask.kt : Playwright `launchPersistentContext` (au lieu de `--user-data-dir` arg rejeté)
- `resolvePlaywrightChromium()` : sélectionne chromium-1228 (v149) car chromium-1161 crashe (ELF)
- Code robuste : waitForLoadState, guards connect/runAll, polling recoverable
- build.gradle.kts : plugin container-provision 0.0.1 appliqué au root + DSL configuré
- 2 commits pushés : `c4a752e`, `f5e045c`
- Fenêtre Chromium + Colab ouverte par utilisateur (profil cheroliv.tester authentifié)
- *RESTE* : Run end-to-end à valider (gradle-local corrigé, à re-tester sur Colab)

=== S005 — Contournement blocage Google + arch plugin indépendant (2026-07-01)
- *Blocage Google contourné* : Chromium snap en subprocess normal + connectOverCDP
  (Playwright launchPersistentContext était détecté par Google via --enable-automation)
- Flags anti-détection : --disable-blink-features=AutomationControlled, --no-first-run
- Profil Chromium snap Profile 2 (authentifié Google Colab) via --user-data-dir + --profile-directory
- Run all : raccourci clavier Ctrl+F9 (bypass shadow DOM Polymer de Colab)
- Polling git : git ls-remote cheroliv/office.git détecte changement HEAD sha (remplace polling DOM fragile)
- Architecture plugin indépendant (article 0124) :
  ** container-provision-plugin/settings.gradle.kts créé (build autonome)
  ** container-provision-plugin/gradlew + gradle/wrapper/* créés
  ** include("container-provision-plugin") retiré de la racine
  ** maven-publish ajouté (Gradle 9.x ne l'applique plus automatiquement)
  ** kotlin 2.3.20 version explicite
- Pipeline Gradle→Playwright→Chromium→Colab validé (30 min sans crash)
- *RESTE* : Notebook ne push pas (secrets Colab à vérifier, ou erreur de cellule)

== Prochaine session

* CP-1d : Déboguer pourquoi le notebook Colab ne push pas (secrets, Ollama, cellule en erreur)
* Voir PROMPT_REPRISE.adoc pour procédure de relance détaillée
