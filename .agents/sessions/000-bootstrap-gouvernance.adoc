= Session 000 — Bootstrap gouvernance container-provision-gradle + code initial

Date : 2026-06-28
Score : 8/10

== Objectif

Créer le borough container-provision-gradle (N0) pour provisionner des conteneurs
et runtimes LLM, en priorité la traduction i18n bakery via Colab + Playwright.

== Actions réalisées

1. Création structure projet (settings.gradle.kts, build.gradle.kts, plugin module)
2. ContainerProvisionPlugin.kt — registre 3 tâches :
   - installPlaywright (NpxTask → npx playwright install chromium)
   - npmInstallPlaywright (NpmTask → npm install)
   - provisionColabTranslation (Exec → node colab-worker.mjs)
3. ContainerProvisionExtension.kt — DSL (colabNotebookUrl, targetLangs, siteRepoUrl)
4. playwright/colab-worker.mjs — script Node.js Playwright Chromium
5. colab/start-ollama-tunnel.ipynb — notebook Colab autonome
6. .agents/ (AGENT, INDEX, BACKLOG)
7. Enregistrement container-provision-gradle dans AGENTS.adoc racine (globaux)
8. Nettoyage historique ALGER → dashboard-plugin dans dashboard-gradle

== Décisions

- Architecture : Node.js + Playwright (NpxTask), pas docker-java ni Playwright Java
- Colab notebook autonome : Ollama → translategemma:4b → JDK → bakery plugin → push
- Pas de tunnel/Cloudflare : tout tourne sur le GPU Colab

== Prochaine session (001)

- CP-2 : Tests Playwright + auth Google
- Finaliser gestion user-data-dir Chrome
