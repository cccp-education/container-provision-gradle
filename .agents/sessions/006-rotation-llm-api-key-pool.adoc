= Session 006 — Rotation LLM api-key-pool + abandon Colab/Playwright
:session: 006
:date: 2026-07-14
:type: dev + arch
:score: 7/10

== Objectif

Reprendre la traduction i18n de cheroliv.com (fr→en) via bakery + Ollama.
Abandon de l'approche Colab/Playwright (trop fragile, 5 sessions sans résultat)
au profit d'une rotation locale sur les ports 11437-11465 avec api-key-pool.

== Ce qui a été fait

=== 1. Diagnostic blocage CP-1d (Colab notebook)

Le notebook `start-ollama-tunnel.ipynb` avait des magic commands Jupyter (`!`, `%`)
indentées dans des blocs Python `if` — Jupyter les rejette en `SyntaxError`.

Corrections appliquées :
- Cellule 16 (Gradle download) : `!curl`/`!unzip` → `subprocess.run([...], check=True)`
- Cellule 21 (push git) : `!git remote set-url`/`!git push` → `subprocess.run([...], cwd=SITE_ROOT)`

Commit `75906f1` poussé sur `cccp-education/container-provision-gradle`.

=== 2. Abandon Colab/Playwright

Après 30 min de polling git sans push (2e run), décision d'abandonner l'approche
Colab+Playwright. Trop de points de défaillance : shadow DOM Polymer, automation
aveugle, secrets Colab, magic commands, notebook 600MB repo clone.

=== 3. Intégration api-key-pool dans bakery-plugin

Utilisation du borough Liberty (`api-key-pool-gradle`, N0) pour la rotation LLM
sur toute la plage 11437-11465 avec `gemma4:31b-cloud`.

Modifications bakery-plugin (commit `ac92cf9` sur `cccp-education/bakery-gradle`) :

- `build.gradle.kts` : BOM 0.0.1 → 0.0.8 + dépendance `education.ccp:api-key-pool`
- `IaConfig.kt` : ajout `portRange: IntRange?` pour configurer le pool
- `PooledOllamaLlmService.kt` (nouveau) : rotation via `ApiKeyPool.getNextKey()`
  avec fallback — si un port échoue, essaye le suivant sur toute la plage
- `ContentTaskRegistrar.kt` : utilise `PooledOllamaLlmService` si `portRange` est
  configuré, sinon `OllamaLlmService` (compatibilité descendante)

=== 4. Validation

Test sur 13 fichiers `.adoc` (blog/2019) :
- 13/13 traduits, 0 erreurs
- `gemma4:31b-cloud` sur ports 11437-11465 (round-robin)
- BUILD SUCCESSFUL en 5m45s

Problème restant : le rendu AsciiDoc perd la structure (titre vide, contenu aplati).
Bug séparé du `PivotArticle` parser/renderer, pas lié au pool.

== État en fin de session

- bakery-plugin commité + poussé sur `cccp-education/bakery-gradle`
- container-provision-gradle notebook corrigé + poussé sur `cccp-education`
- Rotation LLM validée sur 13 fichiers
- Traduction complète 635 fichiers non lancée (temps estimé ~5h)

== Bugs connus restants

. Parser AsciiDoc : rendu cassé (titre vide, contenu aplati) — bug `PivotArticle`
. Traduction complète 635 fichiers à lancer
. Publication bakery-plugin 0.0.4 (avec api-key-pool) sur Maven Central

== Fichiers modifiés

=== container-provision-gradle (commit `75906f1`)
- `colab/start-ollama-tunnel.ipynb` — magic commands → subprocess.run

=== bakery-gradle (commit `ac92cf9`)
- `bakery-plugin/build.gradle.kts` — BOM 0.0.8 + api-key-pool
- `bakery-plugin/src/main/kotlin/bakery/llm/IaConfig.kt` — portRange
- `bakery-plugin/src/main/kotlin/bakery/llm/PooledOllamaLlmService.kt` — nouveau
- `bakery-plugin/src/main/kotlin/bakery/ContentTaskRegistrar.kt` — pool wiring

== Décisions techniques

. *api-key-pool > single endpoint* : La rotation sur 29 ports (11437-11465)
  distribue la charge et évite les points de défaillance uniques. Le fallback
  essaie tous les ports avant d'échouer.
. *Local > Colab* : L'approche locale (Ollama sur ports locaux + bakery Gradle)
  remplace Colab+Playwright. Plus simple, plus fiable, plus rapide à itérer.
. *gemma4:31b-cloud > translategemma:4b* : Modèle autorisé par AGENTS.adoc,
  déjà disponible sur la plage 11437-11465.